# 개발 파이프라인

- 작성일: 2026-08-28 (KST) — 갱신: `npm run ci` 초록 기준 (test 243 · golden 32 · contrast 31)
- 상위: `02_제품/01_제품_구조.md`, `lib/flow.ts` 단일 출처
- 검증: `lib/flow.test.ts` 8건이 FLOW 8단계와 화면 루프·Agent 타임라인의 1:1을 강제, `lib/diagrams.test.ts` 6건이 하드코딩 차단

## 1. 런타임 파이프라인 — 8단계 단일 출처 (lib/flow.ts: FLOW)

```
발화 입력(사람) ── 사람
    │
    ├─[0단] 라우팅    모델  키워드 기준선 대 LLM 비교(evidence 필터, 불일치 시 키워드 우선)  ── lib/skills.ts + lib/ai/agent.ts
    ├─[1단] 추출      모델  발화 → {nationality, visa, hireDate, departureDate, monthlyWage} + evidence 원문 대조  ── lib/ai/agent.ts
    ├─[2단] 판정      코드  입력 + 2026 상수 → Finding[]  ★ 모델 금지구역 — lib/rules만
    ├─ 가드레일       코드  G1–G8 + G8 모순·G7 기한·G3 범위 등  ── lib/harness/guardrails.ts
    ├─ 온톨로지       코드  A-Box를 T-Box 66·관계27·공리8(강제 7/8) 대조  ── lib/ontology/*
    ├─ 답변 조립      코드  Finding[] → Answer (순수, narrate)  ── lib/narrate.ts
    └─[3단] 번역      모델  Answer → 모국어 (숫자 보존 검증)  ── app/api/narrate + lib/ai/contract.ts
```

**모델 3곳(0·1·3단)은 evidence·숫자 보존 계약을 통과해야 화면에 나간다. 통과 못하면 차단·원문 폴백 — 조용한 실패 금지. 2단은 순수 함수만, Math.random/new Date 금지(예외 1곳 page.tsx today).**

## 2. ★ 모듈 경계 — 단방향 의존 (변함 없음, 더 엄격)

```
lib/rules/   ──▶ (아무것도 import하지 않음. 상수와 자기 자신뿐)
lib/flow.ts  ──▶ (사실만, 시각 값 손) — 화면·다이어그램 모두가 이 배열만 읽는다
lib/ai/      ──▶ lib/rules/·lib/flow.ts (Finding·FLOW 타입을 받아 쓴다, 상수 import)
app/         ──▶ 셋 다 (FlowDiagram·다이어그램은 lib/flow·constants만)
```

> **`lib/rules/`는 `lib/ai/`를 절대 import하지 않는다.** 판정은 코드가 한다. `lib/flow.ts`는 도메인 값(7일·3년·G8)을 손으로 박지 않는다 — `lib/rules/constants-departure.ts:기한` 등에서 읽는다.

## 3. 현재 디렉터리 (실물 — `find service -type f | grep -E "page|_flow|_diagrams|lib/" | sort`)

```
service/
  app/
    page.tsx                     콘솔 14뷰·6탭, FLOW 단일출처 참조, today 유일 new Date 허용  ✅
    _agent.tsx                   Agent 실행 뷰·타임라인 0→3, evidence 노출·되묻기  ✅
    _flow.tsx                    FlowDiagram SVG 8단계 (lib/flow.ts만, 가로/세로) — 현재 미렌더(요청)  ✅ 코드
    _diagrams.tsx                Deadline/Money/Nationality 3종 (기한·금액·명단만, 하드코딩 0) — 현재 미렌더  ✅ 코드
    _graph.tsx                   3D 그래프 원둘레+방사형·id 지터, 66노드  ✅
    _ui.tsx / _tabs.tsx / _views.tsx  UI·탭·뷰, Wrap readme·첫방문·용어사전·요약·모션  ✅
    globals.css                  토큰·모션 --dur-fast/base + reduce 0  ✅
    api/agent/route.ts           0·1단만 서버, 판정·가드는 클라이언트 재사용  ✅
    api/narrate/route.ts         3단 번역 + 숫자 보존  ✅
  lib/
    flow.ts                      ★ 8단계 단일 출처 (사람/모델/코드, 보는곳)  ✅
    flow.test.ts                 8건 — 단계·행위자·view/tab·금지·일치 강제  ✅
    diagrams.test.ts             6건 — import·리터럴 차단·출처  ✅
    skills.ts                    레지스트리 + 키워드 라우터 39트리거  ✅
    harness/  core.ts             gate?:'env' 추가, runSelfTest 6체크  ✅
              registry.ts         gate:env 3종(payslip-extractor·departure-intake·narrator)  ✅
              guardrails.ts       G1–G8 8종·LEVEL 6종  ✅
    rules/    types.ts            Finding 정렬·집계  ✅
              constants-2026.ts   S1 2026 상수  ✅
              constants-departure.ts S2 상수·3명단(21·1·10)·기한(30/7/14/3년/5년)·요율표  ✅
              payslip.ts          S1 11룰  ✅
              departure.ts        S2 5룰, today 주입·addDays epoch만  ✅
              departure.test.ts   21건  ✅
              payslip.test.ts     17건  ✅
    ai/       contract.ts         숫자 보존 검증  ✅
              providers.ts        Anthropic/Ollama 스위치, 시한 60s/180s, temp0  ✅
              agent.ts            라우팅·추출 계약 20 tests  ✅
    ontology/ schema.ts 66·관계27·공리8(7/8) · abox.ts · abox.test.ts 8건  ✅
    narrate.ts                    답변 조립 7건  ✅
    cases.ts                      6픽스처(S2-01 2026-10-15)·demonstrates  ✅
    standards.ts                  근거 10종(원본1·판례1·2차8)  ✅
    samples.ts                    합성 명세서 3종  ✅
  scripts/  verify-golden.mjs 32 cases  · contrast.mjs 31 · scan.mjs 111  · smoke.mjs(신설) ·
            playwright-audit.mjs 14뷰 1440/390 dead 0 · verify-flow-motion.mjs 8+6+reduce+Ollama
```

총 243 tests pass (flow 8 + diagrams 6 + contract 8 + agent 20 + rules 38 + harness 8 + ontology 8 + narrate 7 + 기타). `npm_config_cache=/tmp/pc-npm-cache npm test` 243 pass.

### 키워드 라우터는 임시방편이 아니다

`routeByKeyword`는 baseline·폴백으로 계속 남는다. LLM 라우터가 붙어도 `lib/flow.test.ts`는 두 라우터의 병행(키워드:LLM 비교, 불일치 시 키워드 우선)을 강제한다. 0단을 갈아끼워도 하네스 화면은 그대로다.

## 4. 공유 타입이 만든 것 (동일)

`Finding`에 `deadline·amountRange` 추가로 S2 청구 판정을 같은 UI로, `sortFindings·recoverableTotal` 공용화로 스킬 확장 시 UI 재사용.

## 5. 품질 게이트 — 배포 전 반드시

| # | 게이트 | 명령 | 현재 |
|---|---|---|---|
| 1 | 판정·계약·흐름·다이어그램 단위 테스트 | `npm_config_cache=/tmp/pc-npm-cache npm test` | 243 pass |
| 2 | 타입·빌드 | `npm_config_cache=/tmp/pc-npm-cache npm run build` | ok |
| 3 | 골든셋·대비·스캔 | `npm_config_cache=/tmp/pc-npm-cache npm run verify` / `scan` | 32/31/0 |
| 4 | Flow/다이어그램 하드코딩 차단 | `npm test` 내 14건 | pass |
| 5 | 콘솔 육안 | Playwright 1440/390 + reduce | dead 0 |

3번 상수 검증은 `연금명단_교차검사()`가 매 실행·CI에서 700만원 오답을 잡는다.

## 6. 환경변수 — 게이트로 정직

| 키 | 어디 | 용도 | 게이트 |
|---|---|---|---|
| `ANTHROPIC_API_KEY` | Vercel env | 배포 0·1·3단 | `gate:env` → 미연결 시 ◐(대기) |
| `OLLAMA_URL` / `OLLAMA_MODEL` | `.env.local` / 내부망 | 로컬 0·1·3단 | 동일 |

`live:false gate:env` 3종은 구현됨—연결 시 `●` 실동작, 미연결 시 `◐` 대기(○ 예정과 구분). VerifyTab·OrgView·HarnessView 모두 동일 문법(●/◐/○)으로 provider 받아 그린다. `live:true` 4종(라우터·판정부)은 항상 ●.

## 7. 배포 파이프라인 (변함 없음)

`git push → Vercel service Root Directory` → 프로덕션 URL. 9/7 11:00~9/11 23:59 무중단 결격 — push 금지. 최초 1회 `vercel --prod` 대화형.

## 8. 작업 순서 (갱신)

| 기간 | 한 일 | 실측 |
|---|---|---|
| 8/20 | 파이프라인 구조 | — |
| 8/26 | 모바일 골격·온톨로지 3D | 390 330px |
| 8/27 | 화면 결함·타입·도형 표식 | contrast 30→31 |
| 8/28 | 3단 번역·Agent 0·1단 gate | test 199→243, Ollama 3종 |
| 8/28 | FLOW 단일출처·3다이어그램(코드)·모션·문서 4종 | flow 8·diagrams 6 |
| 8/28 | Registry gate·VerifyTab/OrgView ◐·_graph 주석 정정 | 1440/390 재실측 |
| 9/01~03 | 원고 2종(A7·A8) — 이 문서가 재료 | — |
| 9/07 | 제출·증빙·태그 | — |

## 9. 잠근 것

- 8단계 FLOW 단일출처, `rules→ai` 금지, Finding 공용, gate:env 3종, 5게이트, 스킬 레지스트리
